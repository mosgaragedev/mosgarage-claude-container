# CLAUDE.md — {{PROJECT_NAME}} Agent Instructions

> Auto-generated by agent-template init script.
> Edit the sections marked with TODO after generation.
> This file is read by Claude Code at the start of every session.

---

## Project Identity

**Name:** {{PROJECT_NAME}}
**Type:** {{PROJECT_TYPE}}
**Stack:** {{TECH_STACK}}
**Repo:** {{REPO_URL}}
**Developer:** Solo — prioritize clean, readable, maintainable code over clever abstractions
**Open source:** {{IS_OPEN_SOURCE}}

---

## Monorepo / Repo Structure

```
{{REPO_STRUCTURE}}
```

---

## Tech Stack — Exact Versions

<!-- TODO: Fill in your actual stack after generation -->

| Layer | Technology | Notes |
|---|---|---|
| Language | {{LANGUAGE}} | Strict mode always on |
| Framework | {{FRAMEWORK}} | |
| Database | {{DATABASE}} | |
| Package manager | {{PKG_MANAGER}} | Always use this — never switch |

---

## Coding Standards

### Language-specific rules
<!-- TODO: Add rules for your specific language/framework -->

### File Naming
<!-- TODO: Fill in your naming conventions -->

### API Design
<!-- TODO: Fill in if this project has an API -->

---

## Module Inventory

<!-- TODO: Update status as modules are completed -->
<!-- Use: 🔲 Not started | 🚧 In progress | ✅ Done -->

| Module | Status | Spec file |
|---|---|---|
| Example | 🔲 Not started | `docs/specs/example.spec.md` |

---

## Environment Variables

See `.env.example` for full list. Never hardcode secrets.

---

## What NOT To Do — Hard Rules

<!-- These are universal rules that apply to every project -->

- ❌ Never use `any` in TypeScript (or equivalent in your language)
- ❌ Never commit `.env` files — only `.env.example`
- ❌ Never skip input validation on user-supplied data
- ❌ Never return sensitive fields (passwords, tokens, secrets) in API responses
- ❌ Never use `{{PKG_MANAGER_WRONG}}` — always use `{{PKG_MANAGER}}`
- ❌ Never write raw SQL — use the ORM/query builder
- ❌ Never use `console.log` in production code — use the structured logger
- ❌ Never push directly to `main` — always use a feature branch

---

## Key Commands

```bash
# Install dependencies
{{CMD_INSTALL}}

# Start development
{{CMD_DEV}}

# Type check
{{CMD_TYPECHECK}}

# Lint
{{CMD_LINT}}

# Run tests
{{CMD_TEST}}

# Build
{{CMD_BUILD}}
```

---

## Agent Behavior Guidelines

When running autonomously on this project:

1. **Read the relevant spec** in `docs/specs/` before implementing any module
2. **Run `{{CMD_TYPECHECK}}`** after every file change — fix all errors before moving on
3. **Run `{{CMD_LINT}}`** before finishing any task
4. **Write tests** for all route handlers and utility functions
5. **Update module status** in this file when a module is completed
6. **Commit messages** follow Conventional Commits: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`
7. **One concern per commit** — do not bundle unrelated changes
8. **If blocked**, leave a `// TODO(agent): description` comment and continue
9. **Never change schema/migrations** without following the migration guide in `docs/`
10. **Check `docs/PRD.md`** if a requirement is ambiguous — do not guess
