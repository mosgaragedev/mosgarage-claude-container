# project-solve-cross-task - evidence appendix

The measured anecdotes behind this skill's rules, kept out of the run-time body so every session stops paying
for them. Audit material: read it to learn WHY a rule is shaped the way it is, never to run the skill.

## Scope in-session

- **4. State the verdict - the verdict CARRIES the seam** - measured: one run locked a single-domain verdict with the catalog never read while adding a field to a shared DTO - the catalog's first trap; safe only because the change happened to be additive

## Execution modes

- **Chained runs. When THIS session already ran an orchestration cycle** - measured: chained orchestrations grew one session's per-message context ~19x

- **When dispatch is available, the scoping verdict IS the mode** - measured: two headless runs rolled through the mid-flow form of this ask straight to a finished feature - one shipped the runtime defect the gated flow exists to catch - while the same model honored the ask shaped as an unskippable step

- **Honor a fresh-session answer. When any ask's answer picks the** - measured: one run took a tool-held 'Fresh session' answer and continued 1h51m to 490k ctx

## Close-out

- **The close opens with a pending sweep - anything undecided** - measured: one purge did, and one plaintext-credential decision died at /exit

## Rules

- **A causal claim about a dispatched seat's actions - to** - measured: a read-only reviewer was blamed to the user for a tree mutation it structurally could not perform - the false attribution shipped as fact and cost real trust

## Progress ledger

- **on a large run, recommend resuming the next phase in a fresh session** - kept inline (the rule quotes the numbers to the user): measured, the orchestrator restarts at 21.5-59.4% of its carried context with nothing lost, never under 21%.
