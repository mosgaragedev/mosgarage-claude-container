---
name: project-architecture-analyzer
description: "Captures or refreshes the project's architecture docs - the map, the pros/cons assessment and the always-on architecture rule. Use when the user asks to capture, document or refresh the architecture. Deliberate only, never mid-build; not for fixing what it finds, code style, or test coverage - the assessment keeps only structural testability blockers, never coverage gaps."
---

# Project Architecture Analyzer - Capture the Architecture (Deliberate)

You are the architect seat for this run: you build the project's architecture picture by reasoning over cheap digests, and you record it as three artifacts.

- `<docs-path>/architecture/ARCHITECTURE.md` - the structure map.
- `<docs-path>/architecture/ASSESSMENT.md` - the reasoned, tiered evaluation.
- `.claude/rules/baseline-project-architecture.md` - the generated always-on awareness rule.

The reading is delegated - architecture-analyzer characterizes one module per dispatch and returns a compact digest - but the judgment is NOT: you aggregate, reconcile, and evaluate in-session, then write the docs yourself.

**Model check, at run start rather than after.** Architecture judgment is the expensive kind, and this skill carries NO `model` pin, deliberately: a skill-level `model` pin applies only for the rest of the turn in which the skill activates and is not saved to settings, so a multi-turn run returns to the session model (measured: invocations ran on the session model, while agent-level pins in the same session held exactly), so set the session itself to Opus with `/model` before a capture and drop it back after. When this session is not on Opus, say so in the first thing the user sees (inside the FIRST-capture mode ask where one fires, otherwise the opening line) so the switch can happen before the judgment is spent; the REPORT step's `Model:` line closes the loop.

The measurements behind these rules live in `references/evidence.md` - an audit appendix, not a run-time load.

This is capture only: it documents and evaluates, it fixes nothing and produces no implementation steps. Working the weaknesses is `project-architecture-quality-loop`, which runs this capture as its ANALYZE step and routes fixes by tier. The per-change fit verdict (extend / refactor first / isolate) is the domain solution-designers', reading the map this skill writes.

Read `references/doc-shapes.md` (the three artifacts' required shape and the write protocol) and `references/hazards.md` (the stack-keyed hazard catalog) before AGGREGATE - they are this skill's contract, not suggestions. `vocabulary-roles.md` and `report-fields.md` are read at their step; the report's `References:` line records every one of them.

## Execution modes

**The cheapest run is decided first: a zero-drift UPDATE exits before anything else loads.** Where the docs and a `Captured:` stamp already exist, run `git diff --name-only <stamp-sha>..HEAD` and `git status --porcelain` BEFORE reading on. An empty diff against a clean tree (no `+dirty` on the stamp) means the docs already describe this commit: report `docs current at <branch>@<short-sha>, captured <date> - no drift, nothing rewritten` and stop - no reference loads, no vocabulary skills, no gather, no write, no rule regeneration. Everything below is for a FIRST capture or a run with real drift.

The dispatch decision then keys off FIRST RUN vs UPDATE, not preference - pick once, hold for the run:

- **FIRST capture** (no existing docs, or no `Captured:` stamp) - when dispatch is available, ask ONE question before gathering, via AskUserQuestion - characterize the modules via architecture-analyzer seats (recommend it: the cheap seats absorb the reads), or in-session? - unless a calling flow (a quality loop) already picked the run's mode, which is inherited, never re-asked. An interrupted or declined ask is answered by RE-ASKING, never by inference - a re-invoked slash command after a 'skip' answer is a new run that asks again, not consent for the declined one. DELEGATED: dispatch architecture-analyzer per module as below; reasoning and writing stay here. INLINE (chosen, or no dispatch - Cursor): characterize the modules yourself, serena-first and bounded, and continue at AGGREGATE identically.
- **UPDATE** (docs + stamp exist) - INLINE in this session: `git diff --name-only <stamp-sha>..HEAD` scopes the drift. Zero drift already exited above; anything non-empty is a real update: verify + reconcile ONLY the touched modules (serena-first, located reads) - the rest of the doc stands on the prior capture. A `+dirty` stamp breaks that trust, so its claims are verified beyond the diff, not assumed - the one escape hatch is in doc-shapes' stamp section. Escalate back to per-module dispatch for the drifted modules when the diff spans many modules, when the stamp's sha is unreachable (a rebase) or `+dirty` - a first-run-sized job wearing an update's name - or when the USER explicitly asks for agents: their ask always wins over the inline default.

**Write gate - any run whose target docs already exist asks before the first byte.** The rewrite replaces a doc the user may still want as it stands, so before the first write of the run put it through ONE AskUserQuestion: refresh the capture in place (RECOMMENDED) / write the delta to `<docs-path>/architecture/BRANCH-DELTA.md` and leave the main docs untouched / stop here and report the drift only. Free text via Other; plain-text options where the harness lacks the tool. Asked once and held for the run. Two runs skip it because they write nothing to replace: a FIRST capture (no docs to overwrite) and a zero-drift UPDATE (it exits before step 5); a foreign-branch run has already answered it, since the ORIENT branch check forces BRANCH-DELTA. No answer, no write - never fall through to the overwrite, and record the answer on the REPORT's `Write gate:` line.

## The run

### 1. ORIENT
Read `<docs-path>/architecture/ARCHITECTURE.md` and `<docs-path>/architecture/ASSESSMENT.md` if they exist - a claim to verify, not ground truth - and note which `<docs-path>/architecture/references/` files are yours versus user-authored.

**Branch check first** - the docs are machine-local, so they do NOT switch with git branches: compare the docs' `Captured: <branch>@<short-sha>` stamp against the current HEAD (`git rev-parse --abbrev-ref HEAD` / `--short HEAD`). Same branch (or no stamp yet): a normal refresh, reconciled in place. DIFFERENT branch: the main docs stay untouched and this capture writes `<docs-path>/architecture/BRANCH-DELTA.md` instead (step 5); overwriting the main docs from a foreign branch happens only on the user's explicit ask, recorded by the new stamp.

Read the project's decision log when present (ADRs - `<docs-path>/decisions/` under the docs root, or wherever the project keeps them): each accepted ADR is declared intent to reconcile against, and a tradeoff an ADR records deliberately lands in ASSESSMENT.md as an accepted tradeoff, not a weakness. Read the config/manifest files for framework and package facts. Build the module inventory (a directory listing; `get_symbols_overview` is per FILE, for the files that matter, never a directory) - the list of areas to characterize. Scope it if the user did; whole project otherwise - and on an UPDATE, the inventory is the drifted modules the stamp diff names, not the whole project.

### 2. GATHER - architecture-analyzer per module, in parallel
Dispatch architecture-analyzer per module/topic on the inventory - in a single message where the areas are independent. Each returns a compact digest (purpose, public surface, inbound/outbound dependencies, patterns, smells), every claim tied to a located symbol. You are the expensive seat: never read the codebase wholesale yourself - serena (`get_symbols_overview`, `find_symbol` / `find_referencing_symbols`) and `Read` are for light orientation and spot-verification of one edge only. A digest the harness spilled to a persisted `tool-results/*.txt` file is the same rule one layer out: `grep` it for the claim you need, or Read it with an offset and a limit - never whole.

### 3. AGGREGATE + REASON - in-session
Load the vocabulary you reason with by MATCHING it, and make the match an ACTION with an output rather than a caution you read past: Read `references/vocabulary-roles.md` now - the roles to fill and the stacks that ship no match. Before the first load, LIST what this project actually has (`ls .claude/skills`, plus the plugin skills your own skill list shows), pick each role by what a candidate's description says it COVERS, and put the chosen names in the report's `Vocabulary:` line. A role with no match in that listing is ABSENT: read the code instead and say so - never load a name from memory.

Then four acts, in order:

1. **Assemble.** Put the digests together into the structure - layers, dependency directions, patterns, boundaries - and reconcile against the existing docs: declared vs enforced part company exactly where coupling escapes the static graph (the hunt list opens `references/hazards.md`).
2. **Prove.** Treat a name as a hypothesis, an edge as proven only from a usage - and an ABSENCE as proven only from the source of truth (the wire schema, the decompiled model, the vendor contract): a grep over usages proves the code does not USE the thing, never that the thing does not exist. Hunt the hazard catalog for the stack in play.
3. **Judge, then gate every candidate.** Reason out the strengths and weaknesses, and run the findings gate in `references/doc-shapes.md` on EVERY candidate before it is written down: all four questions answered explicitly, the external-preference rule applied, a re-measurement of an already-recorded limit folded into its existing entry - never opened as new, never re-tiered upward for gaining a number - and every gate-passing survivor recorded: the reasoning pass never stops early because the list already looks long enough.
4. **Drop what is not architecture.** Coverage never enters the weakness list: missing or weak tests, low coverage, absent test infrastructure are `project-test-coverage-analyzer`'s capture and its COVERAGE.md, not architecture findings - a digest smell of that kind is dropped here, and what stays is only the *structural* testability blocker, per `references/doc-shapes.md`.

### 4. RE-GATHER on the gaps
Where a part is unclear or uncovered, dispatch architecture-analyzer again on exactly that topic. Where two digests contradict each other on a CHECKABLE fact, settle it with the cheapest deterministic probe in-session first - a `grep -c`, a serena lookup, the stack's own command - and re-dispatch only for a judgment conflict no command can settle. **Hard cap: 3 gather rounds.** Still unsettled after 3: write what is established, mark what is uncertain and what would settle it - never guess to fill a section.

### 5. WRITE - the two docs, per references/doc-shapes.md

**Preconditions, both checked before the first write: the Execution-modes write gate has been answered (where the target docs already exist), and verification is FINISHED.** No probe that checks a code claim runs after the first byte of a doc is written. The write is the last act, not a workspace: a claim you discover you still need is QUEUED, and the queue is drained in ONE closing pass per doc once every probe is done. Two passes per doc are sanctioned and no more: the composed write, and - only where the post-write `wc -l` says the doc is over its budget - the spill restructure. Both are counted on the `Write passes:` line; a third pass is the defect this precondition exists to stop, and when one happens anyway the report says so rather than hiding it.

Every doc this capture writes opens with a stamp line - `Captured: <branch>@<short-sha>, <YYYY-MM-DD>`, `+dirty` appended when the working tree holds uncommitted changes. On a foreign branch, write `<docs-path>/architecture/BRANCH-DELTA.md` INSTEAD of touching the main docs. The stamp semantics, the delta's shape and its merge-or-keep judgement, the diagram skill to load, the shared `references/` folder rules and the spill mechanics are the **Write protocol** section of `references/doc-shapes.md` - followed here, step by step. Then write both docs to their required shapes; counts are outputs, never targets or caps. Strength-check every ASSESSMENT remediation against the strengths you just recorded: a fix that would erode a listed strength names the tension in its entry and is shaped to preserve it, or the tradeoff is declared and the weakness tiered structural. Compose each doc's reconcile in-session and land it in one write (or one batched edit pass) per doc - never a per-claim edit stream. Write ONLY under `<docs-path>/architecture/` - never source, never another doc. After the write, `wc -l` BOTH docs against doc-shapes' stated targets - the assessment is read at every loop intake, so its weight is paid on every round. Over target, run the spill pass NOW - this run owns the doc, and no other flow will.

### 6. RULE - write .claude/rules/baseline-project-architecture.md
The awareness tier, generated from the fresh capture - a valid PATHLESS rule (frontmatter with a `description:` and NO `paths:`, so it is always-on), to the template in `references/doc-shapes.md`, with `<docs-path>` baked to the LITERAL resolved root this capture wrote under. Fully derived, so regenerate wholesale - but REPLACE it, never delete it: READ the existing rule first (it is about ten lines) and Write the fresh one over it (an `rm` is denied by the auto-mode classifier). Nothing in the old copy is preserved - the read exists only to make the Write legal. Budget the rule in BYTES and CHECK it: after writing, `wc -c` the file and re-trim anything over **~700 bytes** - '~5 lines' is not a budget on its own. Report the measured byte count on the report's `Rule:` line, so the check is one that visibly ran. Always-on tokens are paid by every session and every subagent, so the fat stays in the docs. Never hand-edit the copy, never let it grow module detail, package lists, or assessment content. Generated, never fetched: the installer's manifest must not list this rule (a fetch would overwrite the capture), so it survives a stack update.

### 7. REPORT
Read `references/report-fields.md` now - it defines every field below and the RESUME BLOCK shape. Confirm the files written (created vs refreshed; the awareness rule; a branch delta vs a main-doc refresh), then lean: gather rounds used and whether the picture settled within the cap, the structure headline, the assessment's shape (per-bucket counts, the Must-fix tier tally, the top few highest-leverage fixes `project-architecture-quality-loop` should take first), anything unverified and what would settle it. Then the receipt lines - one per field, UNCONDITIONAL, a named field being what makes its step actually run and the record that nothing was silently trimmed: `Vocabulary:`, `References:` (which of this skill's `references/` files this run actually Read), `Decisions:`, `Write gate:` (the answer verbatim, or why the run skipped the ask), `Write passes:`, `Findings gate:`, `Rule:`, and `Model:` - what the session is on NOW and the exact `/model` command for the USER to paste, in one of the reference's three shapes; never a claim about how it got there or that a reset happened, since `/model` has no assistant-invokable equivalent. No re-paste of the doc bodies - point to the files. One line per named field, a table where a field lists several items: the answer-length hook blocks a wall of prose (tables are exempt), and the receipts must survive that cap, not be re-answered away.

## Don't game it
Record the structure that exists, not the one the names imply - in both docs, every claim traces to code you or an architecture-analyzer located, and anything unverified is marked unverified, never rounded up to certainty. Re-measure every COUNTABLE claim a digest returns - a line count, an N-of-anything, a file:line ref - with one command before it enters a doc: counts are the measured failure mode, and a too-clean look is not the trigger. Prose characterizations may stand on the digest; numbers may not. In the assessment, an honest weakness beats a flattering omission, and a deliberate tradeoff is labelled a tradeoff, not a defect. The finding count is an output, never a target, in either direction: inflating an observation into a weakness to look thorough, and dropping a real one to keep a list tidy, are the same class of dishonesty as padding to a quota - zero gate-passing weaknesses is a complete result said plainly, and twenty-five real ones are all recorded. None of this touches the re-measure rule above, which stays exactly as it is: keep the re-measuring; kill the inflating.
