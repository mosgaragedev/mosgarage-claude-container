/**
 * Development environment (used by `ng serve`). The base path is relative; the Angular dev server
 * proxies `/api` to the .NET API on http://localhost:5080 - see proxy.conf.json.
 */
export const environment = {
  production: false,
  apiUrl: '/api',
};
