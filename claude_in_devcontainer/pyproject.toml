[project]
name = "claude-in-docker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
]

[tool.setuptools]
# Exclude non-Python directories from package discovery
packages = []

[tool.setuptools.package-data]
# If you have Python packages in the future, list them explicitly here
